import { LightningElement, api } from 'lwc';

export default class SendEmail_SubscribeDashboard extends LightningElement {
    connectedCallback() {
        console.log('SendEmail_SubscribeDashboard running');
    }
}